version: '3.8'

services:
  # client:
  #   build: ./Client
  #   networks:
  #     app-network:
  #   ports:
  #     - "3000:3000"
  #   volumes:
  #     - ./Client:/app
  #   command: npm run dev
    # env_file:
    #   - ./Client/.env

  server:
    build: ./Server
    networks:
      app-network:
        aliases:
          - api
    volumes:
      - ./Server:/app
    command: npm run dev
    ports:
      - "8000:8000"
    env_file:
      - ./Server/.env

networks:
  app-network:
    driver: bridge
